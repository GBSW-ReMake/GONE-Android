# Issue #17 교사 상벌점 발급 QA

> 작성일: 2026-08-23
> 브랜치: `feat/17-point-system`

## 확인 결과

- 교사 역할에서 4번째 탭이 상벌점으로 표시되고, 학생 역할은 스쿨캠핑을 유지하도록 구현했습니다.
- 대상자 추가, 학생별 폼 저장/수정/삭제, 일괄 발급, 완료 화면, 발급 내역 필터, 개인 통계를 구현했습니다.
- 학생 검색 시트에서 학번/이름 검색과 대상자 선택을 확인했습니다.
- `+` 아이콘, 체크 아이콘, 역할 선택 일러스트 에셋 적용을 확인했습니다.
- 버튼 비활성 상태, 통계 카드 간격/숫자 크기, 빈 상태 중앙 정렬을 반영했습니다.
- `git diff --check`를 통과했습니다.
- `flutter build apk --debug`를 통과했습니다.
- `flutter run -d emulator-5554`로 APK 설치 및 디버그 연결을 확인했습니다.

## 미실행 항목

- `flutter analyze`, `flutter test`는 기존 프로젝트 전역 분석 환경 이슈로 별도 검토가 필요합니다.
- Android SDK Command-line Tools가 설치되지 않아 `flutter doctor --android-licenses`는 실행할 수 없습니다. 연결된 에뮬레이터 빌드/실행에는 영향이 없었습니다.
